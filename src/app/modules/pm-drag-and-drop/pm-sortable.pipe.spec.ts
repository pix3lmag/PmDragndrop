import { PmSortablePipe } from './pm-sortable.pipe';

describe('PmSortablePipe', () => {
  it('create an instance', () => {
    const pipe = new PmSortablePipe();
    expect(pipe).toBeTruthy();
  });
});
