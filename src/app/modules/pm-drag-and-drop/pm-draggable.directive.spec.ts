import { PmDraggableDirective } from './pm-draggable.directive';

describe('PmDraggableDirective', () => {
  it('should create an instance', () => {
    const directive = new PmDraggableDirective();
    expect(directive).toBeTruthy();
  });
});
